<?php
$in=file("input.txt",FILE_IGNORE_NEW_LINES);
$s=0;
foreach($in as $l){
    $m=0;
    for($i=0;$i<strlen($l)-1;$i++)
        for($j=$i+1;$j<strlen($l);$j++)
            $m=max($m,intval($l[$i].$l[$j]));
    $s+=$m;
}
echo $s;
